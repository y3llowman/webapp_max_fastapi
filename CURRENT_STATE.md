# Текущее состояние проекта

_Обновлено: 26.09.2026. Идея продукта и правила работы — в [CLAUDE.md](CLAUDE.md). Исходный план и его ревизия — в [TODO.md](TODO.md)._

**Одной строкой:** ИНН → профиль из реестра МСП и ЕГРЮЛ плюс два вопроса кнопками → список обязанностей со сроками только для этой компании → напоминания в чат MAX до закрытия задачи → черновик документа в чат. Всё это работает сквозным сценарием. Проверено тестами на PostgreSQL, в живом MAX не прогонялось.

## Как запустить

| Что | Команда |
|---|---|
| Всё в Docker | `docker compose up --build`: postgres, API на :8000 (отдаёт и собранное мини-приложение), бот вместе с планировщиком |
| API локально | из корня: `python backend/main.py` (`.env` читается из текущей папки; `DATABASE_URL` на localhost:5433) |
| Бот локально | из корня: `python backend/bot/main_longpooling.py` |
| Мини-приложение | `cd miniapp && npm run dev`. Без `VITE_API_URL` работает на моках, подробности — [miniapp/README.md](miniapp/README.md) |
| Тесты | `python -m unittest discover -s tests`. Тесты с БД запускаются только при `TEST_DATABASE_URL=postgresql+asyncpg://…/пустая_база`: схему они пересоздают |

Демо-сроки поверх настоящих: `python backend/seed_demo_tasks.py <ИНН>`. Добавляют просроченную и выполненную задачи.

## Сквозной сценарий

1. **ИНН.** В боте — `/start` или просто 10/12 цифр в любой момент. В мини-приложении — экран «Подключение». Проверяется контрольная сумма, данные берутся из реестра МСП.
2. **Профиль.** Из реестра МСП: название, ОКВЭД, категория, регион, лицензии. Из ЕГРЮЛ (PDF, в фоне): КПП, адрес, директор — для документов. Режима налогообложения и численности в открытых реестрах нет, поэтому бот спрашивает их двумя вопросами кнопками. Мини-приложение, пока ответов нет, показывает плашку «Ответьте на 2 вопроса в чате».
3. **Обязанности.** `materialize_for` берёт из каталога 15 обязанностей только применимые и записывает сроки на год вперёд. Срок, выпавший на выходной, переносится на рабочий день. Меняется профиль — меняется список; бот сообщает, что добавилось и что ушло.
4. **Напоминания.** В 09:00 МСК — по настройке пользователя (по умолчанию за 30, 7 и 1 день и в день срока). После срока — каждый день, пока задачу не закроют. Сроки на одну дату приходят одним сообщением. Кнопки под сообщением: «Сделано», «Завтра», «Не актуально», «Подготовить документ», «Открыть» (экран задачи в мини-приложении).
5. **Документ.** Черновик .docx с реквизитами бот присылает в чат — из мини-приложения или кнопкой под напоминанием.
6. **События реестров.** Раз в месяц реестр МСП сверяется со снимком: исключение, отсутствие в реестре, смена категории (при выходе из микропредприятий появляется задача по ЛНА). ЕРКНМ — только имитация командой.

## Кто что делает — бэкенд (`backend/`)

| Файл | Что делает | Чего не хватает |
|---|---|---|
| `radar/obligations.py` | **Ядро.** Каталог 15 обязанностей: тексты, «куда», формат, основание со ссылкой, штраф, правило применимости (`applies` → ответ «почему вам»), расписание. `next_workday` — перенос с выходных, праздники 2026 года | Постановление о переносах на 2027 год (`DECREE_DAYS_OFF`). СОУТ, ЭПД и ККТ не включены: их сроки из реестров не вычислить. Порог квоты 25–35 человек по региональным законам не учитывается — только федеральный «больше 35» |
| `radar/deadlines.py` | `Profile` и справочники: режимы, диапазоны численности, категории МСП, 89 регионов | — |
| `radar/documents.py` | Черновики .docx: уведомление КНД 1110355 (реквизиты и КБК, суммы и ОКТМО остаются пустыми) и приказ о квоте | Шаблонов только два. У обязанности без шаблона кнопки нет |
| `radar/detectors.py` | `msp_changes`: исключение из МСП, отсутствие в реестре, смена категории. `reconcile_conditions` закрывает исчезнувшие состояния | Детекторы ЕГРЮЛ (`egrul_conditions`, `egrul_changes`) пустые: парсер PDF не читает раздел о записях |
| `radar/render.py`, `radar/templates.py` | Jinja-шаблоны сообщений (HTML) и клавиатура. `deadline.group` пишет «через N дней / сегодня / просрочено на N дней» | Шаблоны законов, банков и сертификатов есть, событий для них нет |
| `radar/catalog.py` | Типы событий: важность, «сразу или в дайджест» | `remind_before` для `deadline` теперь влияет только на статус «Скоро» (3 дня), напоминания идут по настройкам |
| `notifications/worker.py` | Вся работа с БД и MAX от имени радара: снимки реестров, `materialize_for`, `queue_reminders`, `dispatch`, кнопки (`apply_action`), `send_document`, `sync_msp`, демо-триггеры, расписание APScheduler | Заглушки: `weekly_digest`, `check_banks`, `ingest_laws`, `fan_out_feed`, `check_inspections`, `check_certs` |
| `notifications/planner.py` | `NotificationSettings` (тот же JSON, что у экрана «Уведомления»), метки напоминаний, тихие часы | `push` и `email` только хранятся, этих каналов нет |
| `notifications/models.py` | Таблицы радара: `registry_snapshots`, `business_profiles`, `radar_events`, `notifications`, `feed_items` | Миграций нет: новые колонки досоздаёт `NEW_COLUMNS` в `databases/__init__.py` |
| `bot/message_handler.py` | Онбординг (ИНН → карточка → 2 вопроса → итог), `/profile`, кнопки под напоминаниями, `/demo_remind` и `/demo_event` (только при `DEBUG=true`) | Состояние «жду ИНН» хранится в памяти. Цифры ИНН бот понимает и без него |
| `bot/main_longpooling.py` | Точка входа бота; здесь же запускается планировщик и догоняются пропущенные напоминания | — |
| `bot/client.py` | Общий `Bot` для бота, API и планировщика, `send_html`, `bot_id` для кнопки open_app | — |
| `app/api/routes/company.py` | `GET/POST /session`, `/company`, `/company/refresh`. Отдаёт ОКВЭД, категорию, численность, регион, КПП и `needsAnswers` | `counterparties` всегда 0 |
| `app/api/routes/tasks.py` | `/dashboard`, `/calendar`, `/tasks/{id}` (разделы why, steps, how, law, risks со ссылками), `POST/DELETE /tasks/{id}/submitted`, `POST /tasks/{id}/document` | `unread` всегда `false` |
| `app/api/routes/settings.py` | `GET/PUT /settings/notifications` | — |
| `data_fetching/rmsp_client.py`, `egrul_client.py` | Недокументированные API ФНС. ЕГРЮЛ — медленный PDF, возможна капча | Численности и режима в реестре МСП нет |
| `radar/regulations.py`, `law_rules.py`, `law_ingest.py` | Лента законов (сверх ТЗ): правила классификации актов. Импорт в `law_ingest` исправлен | В планировщик не подключены, работают только в `notifications/demo3.py` |
| `seed_demo_tasks.py` | Демо-сроки для защиты | — |

**Мёртвый код от Telegram-шаблона** — не опирайтесь на него, не подключён: `bot/sender.py`, `bot/handlers/`, `backend/worker.py` (RabbitMQ), `app/api/routes/mailing.py`, `telegram.py`, папка `frontend/` (старый SvelteKit; Dockerfile собирает `miniapp/`), `docs/BACKEND_GUIDE.md` (описывает старую схему).

## Мини-приложение (`miniapp/`)

React + Vite, экраны по макетам. Контракт — `src/api/types.ts`, моки — `src/api/mock.ts`.

- **Задача:** чип периодичности, раскрытые «Почему вам» и «Риски», ссылки «Открыть сайт» и «Открыть текст закона» (открываются через `WebApp.openLink`). Кнопки: «Выполнено» и «Подготовить документ» — вторая только у задач с черновиком.
- **Профиль:** ОКВЭД, категория, численность, регион, ИНН, КПП, ОГРН.
- **Дашборд:** плашка «Ответьте на 2 вопроса в чате», пока профиль неполный.
- **Уведомления:** добавлен режим «За 30, 7 и 1 день» (по умолчанию).
- `npm run build` и `tsc` проходят. Вёрстку новых элементов глазами не проверяли.

## Демо на защите

1. Пришлите боту ИНН → карточка → ответьте на 2 вопроса → список обязанностей и ближайшие сроки.
2. «Открыть ленту обязанностей» → задача → «Почему вам», «Подготовить документ» → файл в чате.
3. `/demo_remind` — напоминания по трём ближайшим срокам сразу. Под сообщением — «Сделано» / «Завтра».
4. `/demo_event msp_excluded` — «исключены из реестра МСП». `/demo_event knm` — проверка из ЕРКНМ (честно назвать имитацией).
5. `/profile` → сменить численность → бот пишет, какие обязанности появились или ушли.

## Допущения, которые стоит знать

- Сроки и штрафы сверены с календарём бухгалтера на 2026 год. Если меняется НК — сверять заново. В каждом напоминании есть оговорка «сверяйтесь с календарём ФНС».
- Сроки создаются от сегодняшнего дня вперёд. Прошлые не создаются, чтобы при подключении не пугать просрочками, которые, скорее всего, уже закрыты.
- Задачу, которая перестала касаться компании, удаляем вместе с её будущими сроками. Отметку «Не актуально» пользователя пересчёт профиля не трогает.
- Отправка в MAX не проверена вживую: MAX может не дать написать пользователю, который ни разу не запускал бота. Тогда документ из мини-приложения вернёт 502 и тост «Не получилось». Кнопка `open_app` использует `contact_id` бота — проверить в клиенте MAX.

## TODO

- [ ] Прогнать сценарий из «Демо на защите» в настоящем MAX: open_app с `contact_id`, файл через `InputMediaBuffer`, callback-кнопки, `bot_started`.
- [ ] Посмотреть глазами новые элементы мини-приложения (чип, ссылки, плашка, строки профиля) в светлой и тёмной темах.
- [ ] Численность и режим — автоматически: сервис ФНС «Прозрачный бизнес» (pb.nalog.ru, бывает капча) или открытые данные ФНС (ССЧР, СНР). Тогда вопросы останутся запасным путём.
- [ ] Праздники и переносы на 2027 год в `DECREE_DAYS_OFF`, когда выйдет постановление.
- [ ] Региональная квота для 25–35 человек: справочник регионов, которые её ввели.
- [ ] ЕРКНМ по ИНН вместо имитации: изучить открытые данные proverki.gov.ru.
- [ ] Детекторы ЕГРЮЛ (недостоверность, ликвидация) — нужен разбор раздела записей в PDF.
- [ ] `unread` на дашборде и `counterparties` в профиле — сейчас константы.
- [ ] Лента законов → обязанности: подключить `law_ingest`/`regulations.run_feed` к планировщику (P2).
- [ ] Alembic вместо `NEW_COLUMNS`, когда схема устоится.
- [ ] Решить судьбу мёртвого кода от Telegram (удалить — по решению команды).
