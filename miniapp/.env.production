# Сборка (npm run build, Dockerfile) ходит в API того же FastAPI, что раздаёт статику.
# npm run dev этот файл не читает — там по-прежнему моки или .env.
VITE_API_URL=/api
# Имя бота в MAX — кнопка «В чат MAX» открывает https://max.ru/<имя>
VITE_MAX_BOT_NAME=t267_hakaton_max_bot
